import PageWrapper from '@app/layout/PageWrapper';
import React, { FC } from 'react';

const File: FC = () => {
  return <PageWrapper className="items-center gap-12 p-6">''</PageWrapper>;
};

export default File;
